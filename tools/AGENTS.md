# Diagnostic tools

Tools are standalone evidence-gathering utilities. They may inspect external devices but must not be imported by runtime applications. Keep protocol assumptions explicit, bounded by timeouts, and output raw observations separately from normalized domain contracts.
